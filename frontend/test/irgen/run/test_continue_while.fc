extern int printf(char[] format, ...);

int main() {
    int num = 0;
    while( num < 5) {
        num += 1;
        if(num == 3) {
            continue;
        }
        printf("%d\n", num);
    }
    return 0;
}
